{
  "name": "d1-types",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "typescript": "^5.2.2",
    "wrangler": "^3.15.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --remote",
    "dev": "wrangler dev --remote",
    "deploy": "wrangler publish",
    "db:init": "wrangler d1 execute d1types --file=schema.sql"
  }
}
